<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ismail.sepon.katex">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ismail.sepon.katex.controller.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MayaTest">
        <activity
            android:name="ismail.sepon.katex.activity.TvDetailsActivity"
            android:exported="true" />
        <activity
            android:name="ismail.sepon.katex.activity.DetailsActivity"
            android:exported="true" />
        <activity
            android:name="ismail.sepon.katex.activity.LandingActivity"
            android:exported="true"
            android:label="@string/title_activity_landing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ismail.sepon.katex.MainActivity"
            android:exported="true" />
    </application>

</manifest>